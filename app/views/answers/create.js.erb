$('.create-answer-errors').html('<%= render 'shared/errors', resource: @answer %>');

<% if @answer.persisted? %>
  $('.new-answer #answer_body').val('');
  $('.nested-fields').find('input:text').val('')
<% end %>
